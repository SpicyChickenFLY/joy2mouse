# xinput2mouse
Map xbox game controller input（Stick，Button） to mouse（click，move）

## keyboard mode
allow user to input A-Z,a-z,0-9 by thumb
any allow user to trigger common func key binding 
e.g. 
* Tab / shift + Tab
* 